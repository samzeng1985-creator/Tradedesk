[package]
name = "trade-desk-local"
version = "0.21.0"
description = "Lightweight local-first foreign trade workspace"
authors = ["TradeDesk"]
edition = "2024"

[lib]
name = "trade_desk_local_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
base64 = "0.22"
calamine = "0.36.0"
rusqlite = { version = "0.40.1", features = ["backup", "bundled-sqlcipher-vendored-openssl"] }
rust_xlsxwriter = "0.97.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
tauri = { version = "2", features = [] }
uuid = { version = "1", features = ["v4", "serde"] }
zeroize = { version = "1", features = ["derive"] }
