# Changelog

## 0.14.0 - 2026-08-10

- 新增独立 SQLCipher 恢复密钥库；新建或首次升级工作区时生成一次性显示的高强度恢复密钥，可在忘记密码时本机恢复解锁。
- “数据与安全”页面支持主动轮换恢复密钥，轮换后旧密钥立即失效，工作区密码和恢复密钥均不写入业务数据库或日志。
- 新增加密 `.tdbackup` 备份包，使用 SQLite 在线备份保证一致性，同时封装加密数据库和恢复密钥库；默认保存到“文档/TradeDesk Backups”。
- 解锁页支持从备份包恢复；原工作区在备份成功解锁验证前保留安全副本，无法验证时可撤销恢复。
- 新增加密附件库，单个文件最大 20 MB，可关联工作区、业务单、采购单、生产记录或单证；内容作为 SQLCipher BLOB 保存并记录 SHA-256。
- 单证编辑器新增 900 ms 防抖自动保存，草稿写入 SQLCipher；程序或页面异常退出后再次编辑可自动恢复，正式保存或签发时清理临时草稿。
- SQLCipher 数据库升级至 Schema V11，新增附件与自动草稿表；沿用现有数据库密码，不重加密、不破坏旧工作区。
- 新增恢复密钥加密、错误密钥拒绝、备份包往返、在线数据库备份和附件密文验证测试。

## 0.13.0 - 2026-08-10

- 企业设置升级为多公司档案，每家公司可保存独立 Logo、多个电子签名和电子章。
- 单证与自选配置导出时可选择公司、电子签名、电子章或不使用签章。
- 电子章按方形比例嵌入 PDF，电子签名按横向比例嵌入，页脚自动使用所选公司名称。
- 主数据新增 Excel 全量导出、标准导入模板和批量导入，覆盖产品、客户、供应商、组件库、自选配置及配置明细。
- Excel 导入支持必填项、数字范围、重复编号和配置关联校验；相同业务编号更新，新增编号创建。

## 0.12.0 - 2026-08-10

- 新增“企业设置”，可配置公司名称并上传 Logo、电子签名；图片以数据形式保存在 SQLCipher 加密工作区中，不依赖外部文件路径或云服务。
- 配置单、商业报价单、形式发票、商业发票、装箱单和外贸合同 PDF 统一显示企业 Logo，并在卖方签名位置显示电子签名。
- 所有 PDF 页脚改用可配置公司名称，不再固定显示 TradeDesk。
- 配置报价单将配置编号、组件数量、配置总价放在同一行，并确保六种语言的多单词字段名称保持单行显示。
- 配置总价统一显示为“币种：金额”，例如“USD: 49350.00”，币种不再附着于字段名称。
- 单证编辑预览同步展示企业 Logo、电子签名和公司名称，便于导出前确认。

## 0.11.0 - 2026-08-10

- 修复编辑自选配置时币种输入形成临时代码、触发金额格式化异常并导致白屏的问题。
- 所有需要录入币种的表单统一改为常用币种下拉，覆盖 CNY、USD、EUR、RUB、AUD 等 23 种常用结算币种。
- 自选配置支持以 CNY 组件价格为基础，手工录入“1 CNY = 目标币种”的当日汇率和日期，自动换算各组件单价并冻结汇率快照。
- 配置单 PDF/CSV 增加六语汇率及汇率日期信息，历史外币配置首次编辑时要求重新确认汇率。
- 放宽配置组件的币种过滤：CNY 组件可用于所有外币配置，解决可用组件被提前耗尽、添加按钮在第 6 项后错误禁用的问题。
- 配置明细将“移除”移动至左侧固定列，收窄数量、单价和总价列；常规窗口宽度无需横向滚动即可操作。
- 禁用按钮改用“不可用”光标和明确原因提示，不再显示误导性的运行中光标。
- SQLCipher 数据库升级至 Schema V10，增量保存配置汇率和汇率日期，兼容原有工作区。

## 0.10.0 - 2026-08-10

- 业务单产品行统一支持“标准产品”和“已完成自选配置”，选择配置产品时自动带入配置编号、名称、单位、币种和冻结总价。
- 自选配置必须先保存完成，且币种与业务单一致，才会出现在业务单的可选产品中；后续采购、生产和单证流程继续复用同一产品快照。
- 配置单新增英语、俄语、法语、西班牙语、葡萄牙语和阿拉伯语六语 PDF、CSV 与打印输出，固定表头采用系统内置专业译文，阿拉伯语使用 RTL 排版。
- 组件词库扩展到类别、品名、品牌、规格、单位、备注、配置产品名称和配置说明，并可按六种语言维护人工确认译文。
- 导出前自动检查中文业务词汇的目标语译文，缺失时明确列出待补词条并阻止生成混合语言配置单。
- SQLCipher 数据库升级至 Schema V9，新增词库译文及业务单产品来源字段，并通过隐藏兼容记录保持采购、生产和单证外键链路轻量稳定。

## 0.9.0 - 2026-08-10

- 自选配置产品新增 PDF、CSV 和打印操作，生成包含配置抬头、9 列组件明细、总价、说明和页码的 A4 横向配置单。
- 新增组件录入词库，固化组件类别、品名和品牌；支持提前录入、停用和包含式模糊搜索。
- 保存组件时自动记忆新类别、品名和品牌，下次新建可直接搜索选择，同时继续允许录入新值。
- SQLCipher 数据库升级至 Schema V8，并自动从现有组件资料补齐词库，兼容已有工作区。

## 0.8.0 - 2026-08-10

- 参考天然气发电机组报价样本的 14 个工作表，新增“自选配置”和“组件库”两类主数据。
- 组件库支持品名、分类、型号/规格/材质、默认数量、单位、单价、币种、品牌、备注以及停用管理。
- 自选配置产品支持从组件库选择组合、逐行调整数量和单价、自动汇总金额，并保存可搜索的产品报价清单。
- 保存组合时冻结组件名称、规格、品牌和价格快照；组件后续调价或停用不会影响历史配置。
- SQLCipher 数据库升级至 Schema V7，通过增量建表兼容已有工作区。

## 0.7.0 - 2026-08-10

- 客户档案扩展为分区录入模板：基本信息、三类地址、购买意向、客户分析、优劣势分析以及主要人员和联系方式。
- 客户搜索支持地址、联系人、购买意向和分析内容。
- 新建单证自动复用客户地址；装箱单优先使用收货地址，发票优先使用账单地址。
- SQLCipher 数据库升级至 Schema V6，通过增量字段迁移兼容现有客户资料。

## 0.6.2 - 2026-08-10

- 修复主数据页面产品、客户和供应商表格操作列错位，确保“编辑/停用”与对应数据行垂直对齐。

## 0.6.1 - 2026-08-10

- 修复业务单表格操作列破坏原生单元格布局，确保每行“编辑/归档”与对应业务单垂直对齐。
- Windows Release 改用 GUI 子系统启动，不再同时显示黑色终端窗口。

## 0.6.0 - 2026-08-10

- 新增商业报价单和形式发票，单证中心扩展到五类真实单证。
- 报价单支持 30 天默认有效期、可编辑有效日期、折扣和商业条款。
- 形式发票支持折扣、收款银行资料和非正式税务/报关发票提示。
- 已签发报价可转换为 PI 或合同；已签发 PI 可转换为合同或商业发票。
- 转换直接复制已签发加密快照，不重新读取可能已经变化的主数据。
- 签发 PI 或合同会将仍在报价阶段的业务单推进到订单阶段，不倒退采购或生产状态。
- 新增两套 Typst A4 模板，并完成真实 PDF 生成和逐页视觉检查。
- 保持 SQLCipher Schema V5 向后兼容，不增加新框架、ORM 或后台服务。

## 0.5.0 - 2026-08-06

- 单证中心接入真实 SQLCipher Schema V5 数据。
- 支持商业发票、详细装箱单和外贸合同的创建、编辑、预览、签发、作废和新版本。
- 单证从业务单冻结客户、产品、金额和贸易条款快照。
- 增加数量、金额、箱数、净毛重和 HS 编码校验。
- 使用 Typst 0.15.1 生成专业 A4 PDF，并保存路径、SHA-256 和导出时间。
- 支持 CSV 导出和从最终 PDF 打印。

## 0.4.0 - 2026-08-06

- 新增真实采购单中心，可从业务单按产品行和供应商拆分采购。
- 采购行冻结产品、数量、单位、成本和销售来源快照，并阻止超量分配。
- 每条采购行自动建立原料、开工、生产、质检、包装和可发货六个里程碑。
- 支持采购状态、计划/实际日期、进度、完成数量和异常原因持久化。
- 工作台改用真实订单、采购成本、生产进度与风险数据。
- 数据库升级至 Schema V4。

## 0.3.0 - 2026-08-06

- 新增业务单中心、订单行、金额计算和业务状态。
- 客户、产品、SKU 与单位在保存业务单时固化快照。
- 增加业务单搜索、编辑和保留历史的归档操作。
- 数据库升级至 Schema V3，并使用事务保存业务单和订单行。
- 修正 Release 构建错误连接开发服务器的问题。

## 0.2.0 - 2026-08-06

- 增加首次创建、密码解锁和手动锁定的 SQLCipher 加密工作区。
- 产品、客户和供应商切换为真实本地数据读写。
- 支持三类主数据的新建、编辑、搜索、停用和重启持久化。
- 增加 Schema V2 增量迁移和主数据审计事件。
- 增加错误密码拒绝、加密文件头和持久化测试。

## 0.1.0 - 2026-08-05

- 建立 Tauri 2、React、TypeScript 和 Rust 跨平台骨架。
- 完成工作台、主数据、采购生产里程碑和单证中心纵向切片。
- 建立 SQLCipher 默认加密边界和第一版数据表。
- 增加 Typst 商业发票模板入口。
- 增加 Windows 最小开发环境安装及验证脚本。
